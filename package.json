{
  "scripts": {
    "update-env": "node update-env.js",
    "reset-env": "node reset-env.js",
    "prepare-commit": "npm run reset-env",
    "prepare": "husky install 2>/dev/null || true"
  },
  "dependencies": {
    "husky": "^9.1.7"
  }
}
